<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.goods.mapper.CategoryMapper">
    <select id="selectEnabled" resultType="com.example.goods.model.Category">
        SELECT id, parent_id, category_name, sort_no, status, create_time, update_time
        FROM goods_category
        WHERE status = 1
        ORDER BY parent_id ASC, sort_no ASC, id ASC
    </select>
</mapper>

